# Web framework + ASGI server
fastapi
uvicorn[standard]

# ORM / models (SQLModel sits on top of SQLAlchemy) + async Postgres driver
sqlmodel
asyncpg

# Typed settings loaded from environment / .env
pydantic-settings

# HTTP client (used by FastAPI's TestClient for our smoke tests)
httpx

# Auth: password hashing (bcrypt, used directly) + JWT access tokens
bcrypt
PyJWT

# Anthropic SDK — SOAP note generation (async client, streaming)
anthropic

# Semantic ICD-10 search: local ONNX MiniLM embeddings (no PyTorch, no query-time
# external API) + in-memory cosine. Pulls in numpy + onnxruntime.
fastembed
